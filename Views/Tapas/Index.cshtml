@model IEnumerable<Conexion.Models.Tapa>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.nom_tapa)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.nom_tapa)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_tapa }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_tapa }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_tapa })
        </td>
    </tr>
}

</table>
