@model IEnumerable<Conexion.Models.Nacionalidades>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.tipo_nacionalidad)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.tipo_nacionalidad)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.id_nacion }) |
            @Html.ActionLink("Details", "Details", new { id=item.id_nacion }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.id_nacion })
        </td>
    </tr>
}

</table>
